interface:
  display_name: "Agent Monad"
  short_description: "Repo-specific patterns and workflows for agent-monad"
  default_prompt: "Use the agent-monad repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true